# Just mark the web/ directory as a known source tree
# The HTML/JS/CSS assets are copied to the build dir by the build script.
install_subdir('.',
  install_dir : 'web',
  exclude_directories : [],
  strip_directory : false,
)
